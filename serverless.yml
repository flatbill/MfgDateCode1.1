# generated by @ng-toolkit/serverless
# mod selzer feb2020 node version 12, stage, region,
# stackTag 
service: MfgDateCode

plugins:
  - serverless-apigw-binary

provider:
  name: aws
  runtime: nodejs12.x
  memorySize: 192
  timeout: 10
  stage: stage3
  region: us-east-1
  stackTags:  # Feb 2020
    Name: MfgDateCodeCatApiStack3

package:
  exclude:
   - src/**
   - node_modules/**
   - firebug-lite/**
   - e2e/**
   - coverage/**
   - '!node_modules/aws-serverless-express/**'
   - '!node_modules/binary-case/**'
   - '!node_modules/type-is/**'
   - '!node_modules/media-typer/**'
   - '!node_modules/mime-types/**'
   - '!node_modules/mime-db/**'

custom:
  apigwBinary:
    types:
      - '*/*'

functions:
  api:
    handler: lambda.universal
    events:
      - http: ANY {proxy+}
      - http: ANY /
